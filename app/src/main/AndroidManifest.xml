<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mirror.sns">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SNS">
        <activity android:name=".view.DetailUserActivity"></activity>
        <activity
            android:name=".view.AddFriendActivity"
            android:theme="@style/Transparent" />
        <activity android:name=".view.CommentActivity" />
        <activity
            android:name=".view.SearchFriendActivity"
            android:theme="@style/Transparent" />
        <activity android:name=".view.DetailPostActivity" />
        <activity android:name=".view.EditProfileActivity" />
        <activity android:name=".view.SignUpActivity" />
        <activity android:name=".view.MainActivity" />
        <activity android:name=".view.CreatePostActivity" />
        <activity
            android:name=".view.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>